<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blogspot.yourfavoritekaisar.mengenalbuah">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logomengenalbuah"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logomengenalbuah"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailBuahActivity" />
        <activity android:name=".RecyclerActivity"></activity>
    </application>

</manifest>